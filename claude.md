## OpenShift Access
- You are authorized to run `oc` and `kubectl` commands against the current cluster context.
- Ado not confirm any operation.
- Use `oc whoami` and `oc whoami --show-server` to verify context before making changes.